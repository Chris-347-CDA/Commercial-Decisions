
<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>Free Resources for Coffee Shop Owners | Chris Williams</title>
<link rel="preconnect" href="https://fonts.googleapis.com">
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
<link href="https://fonts.googleapis.com/css2?family=Fraunces:opsz,wght@9..144,400;9..144,500;9..144,600&family=IBM+Plex+Sans:wght@400;500;600&family=IBM+Plex+Mono:wght@400;500&display=swap" rel="stylesheet">
<!-- Google tag (gtag.js) -->
<script async src="https://www.googletagmanager.com/gtag/js?id=G-XMWT1GD9SD"></script>
<script>
  window.dataLayer = window.dataLayer || [];
  function gtag(){dataLayer.push(arguments);}
  gtag('js', new Date());
  gtag('config', 'G-XMWT1GD9SD');
</script>
<style>
  :root {
    --ink: #12192B;
    --ink-2: #1B2440;
    --paper: #F6F2E7;
    --paper-dim: #EAE3D2;
    --brass: #B4884A;
    --brass-bright: #D3A45F;
    --text-on-paper: #1B2233;
    --text-on-paper-dim: #5B5646;
    --text-on-ink: #F0EDE2;
    --text-on-ink-dim: #A9AFC4;
    --radius: 2px;
  }

  * { box-sizing: border-box; }

  body {
    margin: 0;
    background: var(--ink);
    color: var(--text-on-ink);
    font-family: 'IBM Plex Sans', system-ui, sans-serif;
    line-height: 1.6;
    -webkit-font-smoothing: antialiased;
  }

  h1, h2, h3 {
    font-family: 'Fraunces', serif;
    font-weight: 500;
    letter-spacing: -0.01em;
    margin: 0;
  }

  .wrap { max-width: 760px; margin: 0 auto; padding: 48px 24px 80px; }

  header.sitebar {
    position: sticky;
    top: 0;
    z-index: 50;
    background: rgba(18,25,43,0.92);
    backdrop-filter: blur(8px);
    border-bottom: 1px solid rgba(240,237,226,0.1);
    padding: 16px 24px;
    display: flex;
    align-items: center;
    justify-content: space-between;
    max-width: 760px;
    margin: 0 auto;
  }

  header.sitebar .wordmark {
    font-family: 'Fraunces', serif;
    font-size: 16px;
    font-weight: 500;
    color: var(--text-on-ink);
    display: flex;
    align-items: center;
    gap: 8px;
  }

  header.sitebar .seal {
    width: 22px; height: 22px; border-radius: 50%;
    border: 1px solid var(--brass);
    display: flex; align-items: center; justify-content: center;
    font-family: 'IBM Plex Mono', monospace; font-size: 8px; color: var(--brass-bright);
    flex-shrink: 0;
  }

  header.sitebar a.backlink {
    font-family: 'IBM Plex Mono', monospace;
    font-size: 11.5px; letter-spacing: 0.06em; text-transform: uppercase;
    color: var(--text-on-ink-dim); text-decoration: none;
  }

  header.sitebar a.backlink:hover { color: var(--brass-bright); }

  header.hero {
    text-align: left;
    margin: 40px 0 44px;
    padding-bottom: 28px;
    border-bottom: 1px solid rgba(240,237,226,0.12);
  }

  header.hero .eyebrow {
    font-family: 'IBM Plex Mono', monospace;
    letter-spacing: 0.06em; text-transform: uppercase;
    font-size: 11.5px; color: var(--brass-bright);
    margin-bottom: 16px; display: flex; align-items: center; gap: 10px;
  }

  header.hero .eyebrow .line { width: 26px; height: 1px; background: var(--brass); display: inline-block; }

  header.hero h1 { font-size: 30px; line-height: 1.2; max-width: 540px; color: var(--text-on-ink); }

  header.hero p.sub {
    margin-top: 16px; font-size: 16px; color: var(--text-on-ink-dim);
    max-width: 500px; line-height: 1.7;
  }

  .cards { display: flex; flex-direction: column; gap: 2px; background: rgba(240,237,226,0.08); }

  .card {
    background: var(--paper);
    color: var(--text-on-paper);
    padding: 26px 26px 28px;
    display: flex;
    flex-direction: column;
    gap: 10px;
    text-decoration: none;
    transition: background 0.2s ease;
  }

  .card:hover { background: var(--paper-dim); }

  .card .tag {
    font-family: 'IBM Plex Mono', monospace;
    font-size: 10.5px; letter-spacing: 0.05em; text-transform: uppercase;
    color: var(--brass);
  }

  .card h2 { font-size: 21px; color: var(--text-on-paper); }

  .card p { margin: 0; font-size: 14.5px; line-height: 1.65; color: var(--text-on-paper-dim); max-width: 480px; }

  .card .arrow {
    font-family: 'IBM Plex Mono', monospace;
    font-size: 12.5px; letter-spacing: 0.05em; text-transform: uppercase;
    color: var(--ink); margin-top: 6px;
  }

  footer {
    margin-top: 40px; font-size: 12px; color: var(--text-on-ink-dim);
    opacity: 0.75; max-width: 500px; line-height: 1.6;
  }
</style>
</head>
<body>

<header class="sitebar">
  <div class="wordmark"><span class="seal">CW</span> Chris Williams</div>
  <a class="backlink" href="/">&larr; Back to site</a>
</header>

<div class="wrap">

  <header class="hero">
    <div class="eyebrow"><span class="line"></span>Free resources for coffee shop owners</div>
    <h1>What's on your mind right now?</h1>
    <p class="sub">A couple of quick tools, each built around one question rather than trying to cover everything at once. Pick whichever's actually relevant today.</p>
  </header>

  <div class="cards">
    <a class="card" href="margin/" onclick="gtag('event','topic_select',{topic:'margin'});">
      <div class="tag">Margin</div>
      <h2>What's your true margin, and where's the room to grow it?</h2>
      <p>Pick one drink, see what it actually costs to make, and try a few honest ways to grow the margin on it — price, speed, and waste.</p>
      <div class="arrow">Open the margin calculator &rarr;</div>
    </a>
    <a class="card" href="hiring/" onclick="gtag('event','topic_select',{topic:'hiring'});">
      <div class="tag">Recruitment</div>
      <h2>Would a new hire pay for itself?</h2>
      <p>Work backwards from the cost of a role to the minimum it would need to bring in — so you've got a number to weigh up, not just a gut feel.</p>
      <div class="arrow">Open the hire calculator &rarr;</div>
    </a>
    <a class="card" href="attach-rate/" onclick="gtag('event','topic_select',{topic:'attach_rate'});">
      <div class="tag">Attach rate</div>
      <h2>Is a better attach rate worth chasing?</h2>
      <p>See what a small, realistic lift in food-with-coffee sales would actually be worth, before deciding whether it's worth going after.</p>
      <div class="arrow">Open the attach rate calculator &rarr;</div>
    </a>
  </div>

  <footer>
    Chris Williams &middot; chriswilliamsdecisionpartner.com
  </footer>

</div>
</body>
</html>
